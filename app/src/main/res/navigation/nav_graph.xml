<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home_fragment">
    <fragment
        android:id="@+id/home_fragment"
        android:name="com.axel_stein.ap_diary.ui.home.HomeFragment"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_add_log"
            app:destination="@id/edit_log_fragment">
            <argument
                android:name="id"
                android:defaultValue="0L"
                app:argType="long"/>
        </action>
    </fragment>
    <fragment
        android:id="@+id/edit_log_fragment"
        android:name="com.axel_stein.ap_diary.ui.edit_log.EditLogFragment"
        android:label=""
        tools:layout="@layout/fragment_edit_log">
        <argument
            android:name="id"
            app:argType="long" />
    </fragment>
</navigation>